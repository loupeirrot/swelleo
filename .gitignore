# Secrets — ne JAMAIS publier
.env

# Données générées (regénérées automatiquement par GitHub Actions)
data.json
spots/
regions/
sitemap.xml
robots.txt

# Config locale Claude Code (propre à ta machine)
.claude/settings.local.json

# Logs & fichiers système
surfalert.log
__pycache__/
*.pyc
.DS_Store

# Maquettes de travail (références locales, non publiées)
mockup-*.html

# Image source haute résolution (on publie la version .webp légère)
assets/wave.png
